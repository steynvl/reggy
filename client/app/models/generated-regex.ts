export class GeneratedRegex {

  /* plain regex for target language */
  regex        : string;

  /* compiled regex for target language with flags, e.g. python re.compile */
  compiledRegex: string;
}
