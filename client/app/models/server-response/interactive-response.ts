export interface InteractiveResponse {
  stage: string;
  dot: string;
  regex: string;
  mq: Array<string>;

  blue: Array<string>;
  red: Array<string>;
  alphabet: Array<string>;
  observationTable: object;
  exp: Array<string>;
  sta: Array<string>;
}
